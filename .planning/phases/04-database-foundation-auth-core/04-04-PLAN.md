---
phase: 04-database-foundation-auth-core
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - packages/backend/tests/auth.test.ts
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-05
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "Automated tests verify auth signup, signin, signout, session cookie, and 401 on unauthenticated feed"
    - "User can visually confirm the full auth flow works end-to-end in a browser"
  artifacts:
    - path: "packages/backend/tests/auth.test.ts"
      provides: "Integration tests for auth API and feed auth guard"
      contains: "sign-up"
  key_links:
    - from: "packages/backend/tests/auth.test.ts"
      to: "packages/backend/src/routes/auth.ts"
      via: "server.inject to /api/auth/* endpoints"
      pattern: "/api/auth/"
---

<objective>
Finalize backend integration tests for the auth API and feed auth guard (updating the stub from Plan 01), then verify the complete end-to-end auth flow in the browser including the new /profile route and logout button.

Purpose: This plan ensures everything from Plans 01-03 works together. Auth tests catch regressions. The human-verify checkpoint confirms the visual and interactive experience matches the design contract.

Output: Complete backend auth integration tests, verified end-to-end auth flow.
</objective>

<execution_context>
@/home/benja/work/goonster/.claude/get-shit-done/workflows/execute-plan.md
@/home/benja/work/goonster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-database-foundation-auth-core/04-CONTEXT.md
@.planning/phases/04-database-foundation-auth-core/04-RESEARCH.md
@.planning/phases/04-database-foundation-auth-core/04-UI-SPEC.md
@.planning/phases/04-database-foundation-auth-core/04-01-SUMMARY.md
@.planning/phases/04-database-foundation-auth-core/04-02-SUMMARY.md
@.planning/phases/04-database-foundation-auth-core/04-03-SUMMARY.md

<interfaces>
From packages/backend/src/server.ts (after Plan 02):
```typescript
export function buildApp(overrides?: { store?: VideoStore }): { server: FastifyInstance; ready: Promise<void> }
// Now registers: cors (with credentials), authRoutes, feedRoutes (auth-guarded), videoRoutes (auth-guarded)
```

From packages/backend/tests/auth.test.ts (stub from Plan 01):
```typescript
// Contains 7 test cases covering AUTH-01 through AUTH-04, INFRA-02, INFRA-05
// Uses server.inject() pattern from feed.test.ts
// May need adjustments based on actual better-auth response shapes
```

From packages/backend/tests/feed.test.ts (existing test pattern):
```typescript
import { buildApp } from '../src/server.js'
import { MockVideoStore } from './fixtures/MockVideoStore.js'
// Uses server.inject() for HTTP testing
// beforeAll: buildApp + server.ready()
// afterAll: server.close()
```

From packages/backend/src/auth.ts (from Plan 01):
```typescript
export const auth = betterAuth({ ... })
// Endpoints: /api/auth/sign-up/email, /api/auth/sign-in/email, /api/auth/sign-out, /api/auth/get-session
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize backend auth integration tests</name>
  <files>
    packages/backend/tests/auth.test.ts
  </files>
  <read_first>
    packages/backend/tests/auth.test.ts,
    packages/backend/tests/feed.test.ts,
    packages/backend/tests/fixtures/MockVideoStore.ts,
    packages/backend/src/server.ts,
    packages/backend/src/auth.ts,
    packages/backend/src/routes/auth.ts,
    packages/backend/src/routes/feed.ts,
    packages/backend/vitest.config.ts
  </read_first>
  <action>
Update the `packages/backend/tests/auth.test.ts` stub created in Plan 01 to ensure it matches the actual better-auth implementation.

Read the actual auth.ts, routes/auth.ts, and server.ts files to confirm:
1. The exact endpoint URLs (e.g., `/api/auth/sign-up/email` vs other patterns)
2. The expected request body format (JSON with `email`, `password`, `name`)
3. The response body shape (does `sign-up` return `{ user: ... }` or `{ data: { user: ... } }`?)
4. The cookie name and format (is it `set-cookie` with `better-auth.session_token`?)
5. Whether `server.inject` works with better-auth's Request/Response handler or if raw HTTP is needed

Adjust the test file accordingly. The stub already has the correct test structure; the task is to fix any assertion mismatches based on actual implementation details.

IMPORTANT NOTES:
- These tests require a running PostgreSQL database. Use `describe.skipIf(!process.env.DATABASE_URL)` to avoid CI failures on environments without PostgreSQL.
- If `server.inject()` does not work well with better-auth's Request/Response adaptation layer, switch to actual HTTP requests using `fetch` against a running server on a random port. In that case, start the server on a random port in `beforeAll` and use `fetch(`http://localhost:${port}/...`)`.
- The `payload` approach with `server.inject` may need to be `body: JSON.stringify(payload)` with `content-type: application/json` header if Fastify's inject does not auto-serialize.
- If tests fail during execution, debug and fix iteratively. The goal is passing tests, not just a test file that typechecks.

After confirming the test file is correct, run:
```bash
# Start PostgreSQL if not running
docker compose up -d

# Run migrations
cd packages/backend && npx drizzle-kit generate && npx drizzle-kit migrate

# Run tests
DATABASE_URL=postgresql://goonster:goonster@localhost:5432/goonster pnpm --filter backend test
```
  </action>
  <verify>
    <automated>cd /home/benja/work/goonster && pnpm --filter backend typecheck</automated>
  </verify>
  <done>Auth integration tests updated to match actual better-auth implementation. Tests cover signup (AUTH-01), signin (AUTH-02), signout (AUTH-03), session httpOnly (AUTH-04), feed 401 (INFRA-02), and CORS credentials (INFRA-05). Backend typechecks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete auth flow end-to-end</name>
  <files>none</files>
  <action>
Human verification of the complete auth flow. No code changes -- this is a visual and functional verification checkpoint.

Prerequisites before verifying:
1. PostgreSQL must be running (docker compose up -d or local install)
2. Migrations applied: cd packages/backend and run npx drizzle-kit generate then npx drizzle-kit migrate
3. Start both servers: pnpm dev from project root (or pnpm --filter backend dev and pnpm --filter frontend dev in separate terminals)
  </action>
  <verify>Human visual and functional verification of all auth flows listed in how-to-verify section</verify>
  <done>User has confirmed: register, login, logout, session persistence, auth redirect, error messages, /profile route, and mobile viewport all work correctly and match the UI-SPEC design contract.</done>
  <what-built>
    Complete authentication system: PostgreSQL database, better-auth backend with email/password registration and login, httpOnly session cookies, auth-gated feed/video endpoints, React Router with /login, /register, /feed, /profile routes, ProtectedRoute guard, LogoutButton on feed, dark-themed auth UI with floating label inputs.
  </what-built>
  <how-to-verify>
    **Prerequisites:** PostgreSQL must be running. If using docker-compose: `docker compose up -d` from project root. Then run migrations: `cd packages/backend && npx drizzle-kit generate && npx drizzle-kit migrate`.

    **Start the app:** From project root, run `pnpm dev` (or `pnpm --filter backend dev` and `pnpm --filter frontend dev` in separate terminals).

    **Test flow:**

    1. Open http://localhost:5173 in a browser
       - Expected: Redirected to /login (catch-all -> /feed -> ProtectedRoute -> /login)
       - Verify: No flash of feed content during redirect

    2. On /login page, verify visual design:
       - Dark black background, zinc-900 card centered vertically
       - "Log in" heading in white
       - Two floating label inputs (Email, Password) that float on focus
       - "Log in" white button
       - "Don't have an account? Register" link below card

    3. Click "Register" link
       - Expected: Navigates to /register
       - Verify: "Create account" heading, same visual style
       - Verify: "8+ characters" hint below password field
       - Verify: "Already have an account? Log in" toggle link

    4. Register a new account:
       - Enter email: test@example.com
       - Enter password: testpassword
       - Click "Create account"
       - Expected: Button shows spinner, then redirects to /feed
       - Verify: Feed loads with videos
       - Verify: "Log out" button visible in top-right corner of feed

    5. Click "Log out" button
       - Expected: Redirected to /login
       - Verify: Navigating back to /feed redirects to /login (session destroyed)

    6. Log in with the test account:
       - Enter email: test@example.com, password: testpassword
       - Expected: Redirects to /feed

    7. Navigate to /profile
       - Expected: Profile page shows user name and email
       - Verify: "Profile" heading visible
       - Verify: "Log out" button visible

    8. Refresh the browser (F5) on /feed
       - Expected: Still on /feed, still authenticated (session persists via httpOnly cookie)

    9. Open DevTools -> Application -> Cookies
       - Verify: Session cookie exists with httpOnly flag

    10. Try registering again with same email:
        - Navigate to /register
        - Enter email: test@example.com, password: testpassword
        - Expected: "An account with this email already exists" error below email field

    11. Try wrong password:
        - Navigate to /login
        - Enter email: test@example.com, password: wrong
        - Expected: "Incorrect email or password" error

    12. Test mobile viewport (375px wide in DevTools):
        - Verify: Card fills width with mx-4 margins
        - Verify: All touch targets are comfortable (44px+)
        - Verify: Floating labels work on mobile
  </how-to-verify>
  <resume-signal>Type "approved" if the full auth flow works correctly, or describe any issues you observe.</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm --filter backend typecheck` exits 0
2. Auth test file exists with tests for all 8 requirements
3. End-to-end flow: register -> redirect to feed -> log out -> redirect to login -> login with existing account -> wrong password shows error -> unauthenticated access redirects to login
4. /profile route renders ProfilePage stub behind ProtectedRoute
5. LogoutButton on feed calls signOut() and redirects to /login
6. Visual design matches UI-SPEC: dark theme, zinc-900 card, floating labels, white CTA
7. Mobile viewport at 375px works correctly
</verification>

<success_criteria>
- Auth integration tests exist and cover all 8 phase requirements
- User can register, log in, log out, and access the auth-gated feed
- User can visit /profile and see basic user info
- Session persists across browser refresh
- Unauthenticated access redirects to /login without content flash
- Wrong credentials show appropriate error messages
- LogoutButton is accessible on the feed and works correctly
- Visual design matches the UI-SPEC design contract
- Mobile viewport renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-database-foundation-auth-core/04-04-SUMMARY.md`
</output>
