---
phase: quick-260402-hc2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/store/DiskVideoStore.ts
  - packages/backend/src/server.ts
  - packages/backend/src/routes/feed.ts
  - packages/shared/package.json
  - packages/backend/tests/DiskVideoStore.test.ts
autonomous: true
must_haves:
  truths:
    - "DiskVideoStore handles corrupt/missing metadata.json without crashing the server"
    - "createReadStream reuses async metadata lookup instead of synchronous duplicate"
    - "Server uses Fastify logger (pino) instead of console.log"
    - "Shared package exports compiled output, not raw .ts"
    - "Feed route returns structured error envelope for unexpected errors"
    - "Server validates metadata.json existence on startup"
  artifacts:
    - path: "packages/backend/src/store/DiskVideoStore.ts"
      provides: "Error-safe store with cached/shared metadata lookup"
    - path: "packages/backend/src/server.ts"
      provides: "Pino logger usage, startup metadata validation"
    - path: "packages/backend/src/routes/feed.ts"
      provides: "Consistent error envelope for all error paths"
    - path: "packages/shared/package.json"
      provides: "Proper exports pointing to dist/"
  key_links:
    - from: "packages/backend/src/store/DiskVideoStore.ts"
      to: "packages/backend/tests/DiskVideoStore.test.ts"
      via: "existing tests must still pass after refactor"
      pattern: "DiskVideoStore"
---

<objective>
Clean up 6 medium+ tech debt items identified by codebase audit: DiskVideoStore safety, logging hygiene, shared package exports, duplicated logic, error envelopes, and startup validation.

Purpose: Eliminate crash risks and production-readiness gaps in the backend.
Output: Hardened backend code with proper error handling, logging, and package exports.
</objective>

<execution_context>
@/home/bfn/work/goonster/.claude/get-shit-done/workflows/execute-plan.md
@/home/bfn/work/goonster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/backend/src/store/DiskVideoStore.ts
@packages/backend/src/store/VideoStore.ts
@packages/backend/src/server.ts
@packages/backend/src/routes/feed.ts
@packages/shared/package.json
@packages/shared/tsconfig.json
@packages/backend/tests/DiskVideoStore.test.ts
@packages/backend/tests/feed.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden DiskVideoStore — eliminate sync IO, add error handling, remove duplication</name>
  <files>packages/backend/src/store/DiskVideoStore.ts, packages/backend/src/store/VideoStore.ts, packages/backend/tests/DiskVideoStore.test.ts</files>
  <behavior>
    - Test: createReadStream with a valid id returns a readable stream (existing behavior preserved)
    - Test: listVideos with corrupt metadata.json throws a descriptive error (not raw JSON.parse crash)
    - Test: listVideos with missing metadata.json throws a descriptive error (not ENOENT crash)
    - Test: createReadStream with nonexistent id throws "Video not found" (existing behavior preserved)
  </behavior>
  <action>
  Addresses debt items #1 (CRITICAL — unprotected JSON.parse + sync readFileSync) and #4 (duplicated store logic).

  In DiskVideoStore.ts:
  1. Change `createReadStream` signature in VideoStore interface to `async` — return `Promise<NodeJS.ReadableStream>` instead of `NodeJS.ReadableStream`. This is the clean fix: the sync readFileSync exists ONLY because createReadStream was typed as synchronous. Making it async lets us reuse the existing async `findMeta()` method.
  2. Update `createReadStream` implementation to be async: use `await this.findMeta(id)` instead of the duplicated synchronous JSON.parse + readFileSync block.
  3. Wrap `listVideos()` JSON.parse in try/catch — catch both file read errors (ENOENT) and parse errors (SyntaxError). Throw a descriptive error: `"Failed to load video metadata: {original message}"`.
  4. The `findMeta` private method already calls `listVideos()` so it inherits the error handling.

  In VideoStore.ts interface:
  - Change `createReadStream` return type from `NodeJS.ReadableStream` to `Promise<NodeJS.ReadableStream>`.

  Update existing tests in DiskVideoStore.test.ts:
  - Add test for corrupt metadata (write a temp file with invalid JSON, create store pointing to it, expect listVideos to throw with descriptive message).
  - Add test for missing metadata file path.
  - Ensure all existing tests still pass (createReadStream tests may need `await` if any exist — check first, there are none currently so no change needed there).

  IMPORTANT: After changing VideoStore interface, check if any callers of createReadStream need updating. Grep for `createReadStream` — it is only defined in the interface and implemented in DiskVideoStore. If MockVideoStore in tests/fixtures/ exists, update it too.
  </action>
  <verify>
    <automated>cd /home/bfn/work/goonster && pnpm --filter @goonster/backend exec vitest run tests/DiskVideoStore.test.ts</automated>
  </verify>
  <done>createReadStream is async, reuses findMeta (no duplicated sync logic), listVideos wraps JSON.parse in try/catch with descriptive errors, all DiskVideoStore tests pass including new error-handling tests</done>
</task>

<task type="auto">
  <name>Task 2: Fix server logging, startup validation, feed error envelope, and shared package exports</name>
  <files>packages/backend/src/server.ts, packages/backend/src/routes/feed.ts, packages/shared/package.json</files>
  <action>
  Addresses debt items #2 (console.log), #3 (shared exports raw .ts), #5 (unstructured error), and #6 (no startup validation).

  In server.ts:
  1. Enable Fastify's built-in pino logger: change `Fastify({ logger: false })` to `Fastify({ logger: true })` (or `{ logger: { level: 'info' } }` for explicit control).
  2. Replace `console.log('Server listening on port ...')` with `server.log.info('Server listening on port %d', port)`. Since Fastify's listen already logs by default when logger is enabled, you can simply remove the console.log entirely — Fastify logs `{"msg":"Server listening at http://0.0.0.0:3000"}` automatically.
  3. Add metadata.json existence check at startup (in the `isMain` block, after buildApp but before server.listen): use `fs.promises.access(metadataPath)` wrapped in try/catch. If missing, log a fatal error via `server.log.fatal()` and `process.exit(1)`. The metadataPath is `path.join(fixturesDir, 'metadata.json')` — extract fixturesDir computation or pass it. Simplest approach: add a `validateMetadata()` method to DiskVideoStore that checks the file exists and is valid JSON, call it before listen. Or just inline the check in the isMain block using the same path computation already there.

  In feed.ts:
  4. Replace the bare `throw err` on line 38 with a structured 500 response:
     ```
     request.log.error(err, 'Feed request failed')
     return reply.status(500).send({
       error: { code: 'INTERNAL_ERROR', message: 'An unexpected error occurred' },
     })
     ```
     This returns the same envelope shape as the 400 responses. Use `request.log.error` (available because Fastify logger is now enabled) to log the actual error for debugging. Do NOT expose the raw error message to the client.

  In shared/package.json:
  5. Update exports to point to compiled output:
     - `"main": "./dist/types/video.js"`
     - `"types": "./dist/types/video.d.ts"`
     - `"exports": { ".": { "types": "./dist/types/video.d.ts", "import": "./dist/types/video.js" } }`
  6. Add a `"build": "tsc"` script to shared/package.json.
  7. Run `cd packages/shared && pnpm run build` to generate dist/ output.
  8. Verify the backend and frontend still compile by running `pnpm run typecheck` from each package.

  NOTE on shared package: The backend uses `@goonster/shared` via workspace protocol. Currently everything works because bundlers (tsx, vite) can consume raw .ts. After this change, the compiled .js + .d.ts will be used instead. If the backend's tsx dev mode breaks because it was relying on raw .ts imports, add a conditional export: `"exports": { ".": { "types": "./dist/types/video.d.ts", "import": "./dist/types/video.js", "default": "./src/types/video.ts" } }`. But try compiled-only first.
  </action>
  <verify>
    <automated>cd /home/bfn/work/goonster && pnpm --filter @goonster/shared run build && pnpm --filter @goonster/backend exec vitest run && pnpm --filter @goonster/frontend exec vitest run 2>/dev/null; echo "---"; pnpm --filter @goonster/backend run typecheck; pnpm --filter @goonster/frontend run typecheck 2>/dev/null</automated>
  </verify>
  <done>Server uses Fastify pino logger (no console.log), metadata.json validated on startup with fatal exit if missing, feed route returns structured error envelope for 500s, shared package exports compiled .js/.d.ts output, all existing tests pass, typecheck passes across packages</done>
</task>

</tasks>

<verification>
1. All backend tests pass: `cd packages/backend && pnpm exec vitest run`
2. No `console.log` in server.ts: `grep console.log packages/backend/src/server.ts` returns nothing
3. No `readFileSync` in DiskVideoStore.ts: `grep readFileSync packages/backend/src/store/DiskVideoStore.ts` returns nothing
4. Shared package has dist/: `ls packages/shared/dist/types/video.js packages/shared/dist/types/video.d.ts`
5. Feed route has no bare `throw err`: `grep "throw err" packages/backend/src/routes/feed.ts` returns nothing
6. Typecheck passes: `pnpm --filter @goonster/backend run typecheck`
</verification>

<success_criteria>
- DiskVideoStore.createReadStream is async, uses findMeta, no synchronous fs calls
- DiskVideoStore.listVideos wraps JSON.parse with error handling
- Server uses Fastify logger, no console.log
- Server validates metadata.json on startup before accepting requests
- Feed route returns `{ error: { code, message } }` envelope for all error paths including 500s
- Shared package.json exports compiled dist/ output
- All existing tests pass, new error-handling tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260402-hc2-clean-up-medium-tech-debt-diskvideostore/260402-hc2-SUMMARY.md`
</output>
