---
phase: quick
plan: 260402-cj4
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
must_haves:
  truths:
    - "README.md exists at repo root and describes what Goonster is"
    - "README includes setup instructions that work for a new contributor"
  artifacts:
    - path: "README.md"
      provides: "Project overview, setup, and usage documentation"
      min_lines: 40
  key_links: []
---

<objective>
Create a README.md at the repository root that describes Goonster, its tech stack, and how to set up and run the project.

Purpose: Give anyone cloning the repo a clear understanding of what Goonster is and how to get it running locally.
Output: README.md
</objective>

<execution_context>
@/home/bfn/work/goonster/.claude/get-shit-done/workflows/execute-plan.md
@/home/bfn/work/goonster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@package.json
@packages/backend/package.json
@packages/frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md</name>
  <files>README.md</files>
  <action>
Create README.md at the repo root with these sections:

1. **Title and tagline** — "Goonster" with the core value proposition: a mobile-first short-form video player that aggregates content shared by friends from TikTok, Instagram Reels, and YouTube Shorts into a single, friend-curated feed. No algorithm, just people you trust.

2. **Tech Stack** — Brief table or list:
   - Frontend: React 19, Vite 8, Tailwind CSS 4, TanStack Query 5, Zustand, Swiper.js
   - Backend: Node.js 22, Fastify 5, TypeScript
   - Testing: Vitest, Testing Library
   - Tooling: pnpm monorepo

3. **Prerequisites** — Node.js >= 22 LTS, pnpm

4. **Getting Started** — Step-by-step:
   ```
   git clone <repo-url>
   cd goonster
   pnpm install
   ```

5. **Development** — How to run:
   - Backend: `pnpm dev:backend` (runs on :3000)
   - Frontend: `cd packages/frontend && pnpm dev` (runs on :5173)
   - Tests: `pnpm test`
   - Typecheck: `pnpm typecheck`

6. **Project Structure** — Brief monorepo layout:
   ```
   packages/
     backend/    — Fastify API server, static video serving
     frontend/   — React SPA, vertical swipe video player
     shared/     — Shared types and utilities
   ```

7. **Status** — "In active development (Milestone 1 — static content player)"

Keep it concise and practical. No badges, no emojis, no filler. Match the tone of the project — straightforward and builder-focused.
  </action>
  <verify>
    <automated>test -f /home/bfn/work/goonster/README.md && wc -l /home/bfn/work/goonster/README.md | awk '{if ($1 >= 40) print "PASS"; else print "FAIL: too short"}'</automated>
  </verify>
  <done>README.md exists at repo root with project description, tech stack, setup instructions, dev commands, and project structure. At least 40 lines.</done>
</task>

</tasks>

<verification>
- README.md exists and is well-structured
- Setup instructions reference correct commands from package.json
- Tech stack matches what is actually installed
</verification>

<success_criteria>
- README.md present at repo root
- Contains accurate setup and run instructions
- Describes the project clearly for someone encountering it for the first time
</success_criteria>

<output>
After completion, create `.planning/quick/260402-cj4-create-a-readme-file/260402-cj4-SUMMARY.md`
</output>
